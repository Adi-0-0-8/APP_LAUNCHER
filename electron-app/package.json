{
  "name": "launcher-app",
  "version": "1.0.0",
  "description": "App Launcher - Quick access to your favorite apps",
  "main": "main.js",
  "author": "Your Name",
  "scripts": {
    "start": "electron .",
    "build": "echo Building portable version... && if exist dist rmdir /s /q dist && mkdir dist && xcopy /E /I /Y . dist\\AppLauncher\\ /EXCLUDE:build-exclude.txt && echo Build complete! Portable app is in: dist\\AppLauncher"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-packager": "^17.1.2"
  }
}
